name: Docker CI - youtube

on:
  push:
    paths:
      - youtube/Dockerfile
  pull_request:
    paths:
      - youtube/Dockerfile
  workflow_dispatch:

jobs:
   build:
    runs-on: ubuntu-latest
    name: Set up Docker
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Build
        uses: docker/build-push-action@v2
        with:
          context: youtube/
          file: youtube/Dockerfile
          pull: false
          push: false