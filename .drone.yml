---

kind: pipeline
type: docker
name: build-480p

platform:
  os: linux
  arch: amd64

# Each resolution is its own pipeline (Drone's matrix idiom for this
# pipeline format), built from the same Dockerfile but pointed at a
# different pre-transcoded video asset (see
# .github/workflows/video-assets.yml). Actual functional testing (env
# vars, autoplay/unmute behavior, container security posture) happens in
# .github/workflows/test.yml, not here - lint + build is the only gate
# before pushing. Independent pipelines get scheduled onto whichever
# runner is free, so 2 runners run 2 of these at a time, 4 runners run
# all of them at once.
steps:
- name: Lint Dockerfile
  image: hadolint/hadolint:latest-alpine
  pull: if-not-exists
  commands:
    - hadolint --version
    - hadolint Dockerfile*

- name: Build and Push 480p
  image: thegeeklab/drone-docker-buildx
  privileged: true
  settings:
    repo: ${DRONE_REPO}
    purge: true
    compress: true
    use_cache: true
    build_args:
      - BUILDKIT_INLINE_CACHE=1
      - VIDEO_URL=https://github.com/modem7/docker-rickroll/releases/download/video-assets-v1/video-480p.mp4
    cache_from: "${DRONE_REPO}:480p"
    platforms:
      - linux/amd64
      - linux/arm64/v8
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password
    tags:
      - 480p
  depends_on:
    - Lint Dockerfile

# Auto-builds on every push to master that actually touches the shipped
# image (Dockerfile/scripts) - a README-only or CI-config-only commit
# won't trigger a rebuild. "custom" is kept so a rebuild can still be
# forced manually (e.g. to pick up a new video asset) without a code
# change to hang it on.
trigger:
  branch:
    - master
  event:
    - push
    - custom
  paths:
    include:
      - Dockerfile
      - scripts/**

---

kind: pipeline
type: docker
name: build-720p

platform:
  os: linux
  arch: amd64

steps:
- name: Lint Dockerfile
  image: hadolint/hadolint:latest-alpine
  pull: if-not-exists
  commands:
    - hadolint --version
    - hadolint Dockerfile*

- name: Build and Push 720p
  image: thegeeklab/drone-docker-buildx
  privileged: true
  settings:
    repo: ${DRONE_REPO}
    purge: true
    compress: true
    use_cache: true
    build_args:
      - BUILDKIT_INLINE_CACHE=1
      - VIDEO_URL=https://github.com/modem7/docker-rickroll/releases/download/video-assets-v1/video-720p.mp4
    cache_from: "${DRONE_REPO}:720p"
    platforms:
      - linux/amd64
      - linux/arm64/v8
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password
    tags:
      - 720p
  depends_on:
    - Lint Dockerfile

trigger:
  branch:
    - master
  event:
    - push
    - custom
  paths:
    include:
      - Dockerfile
      - scripts/**

---

kind: pipeline
type: docker
name: build-1080p-latest

platform:
  os: linux
  arch: amd64

steps:
- name: Lint Dockerfile
  image: hadolint/hadolint:latest-alpine
  pull: if-not-exists
  commands:
    - hadolint --version
    - hadolint Dockerfile*

- name: Build and Push 1080p (latest)
  image: thegeeklab/drone-docker-buildx
  privileged: true
  settings:
    repo: ${DRONE_REPO}
    purge: true
    compress: true
    use_cache: true
    build_args:
      - BUILDKIT_INLINE_CACHE=1
    cache_from: "${DRONE_REPO}:latest"
    platforms:
      - linux/amd64
      - linux/arm64/v8
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password
    tags:
      - latest
      - 1080p
  depends_on:
    - Lint Dockerfile

trigger:
  branch:
    - master
  event:
    - push
    - custom
  paths:
    include:
      - Dockerfile
      - scripts/**

---

kind: pipeline
type: docker
name: build-2160p

platform:
  os: linux
  arch: amd64

steps:
- name: Lint Dockerfile
  image: hadolint/hadolint:latest-alpine
  pull: if-not-exists
  commands:
    - hadolint --version
    - hadolint Dockerfile*

- name: Build and Push 2160p
  image: thegeeklab/drone-docker-buildx
  privileged: true
  settings:
    repo: ${DRONE_REPO}
    purge: true
    compress: true
    use_cache: true
    build_args:
      - BUILDKIT_INLINE_CACHE=1
      - VIDEO_URL=https://github.com/modem7/docker-rickroll/releases/download/video-assets-v1/video-2160p.mp4
    cache_from: "${DRONE_REPO}:2160p"
    platforms:
      - linux/amd64
      - linux/arm64/v8
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password
    tags:
      - 2160p
  depends_on:
    - Lint Dockerfile

trigger:
  branch:
    - master
  event:
    - push
    - custom
  paths:
    include:
      - Dockerfile
      - scripts/**

---

kind: pipeline
type: docker
name: finalize

platform:
  os: linux
  arch: amd64

# Waits for all four resolution pipelines. Runs even if one of them
# failed (status: failure below) so Slack still gets notified either way,
# but pushrm only pushes the README if everything actually succeeded.
depends_on:
  - build-480p
  - build-720p
  - build-1080p-latest
  - build-2160p

steps:
- name: pushrm-dockerhub
  image: chko/docker-pushrm
  environment:
    DOCKER_USER:
      from_secret: docker_username
    DOCKER_PASS:
      from_secret: docker_password
    PUSHRM_FILE: README.md
    PUSHRM_SHORT: Dockerised Rick Roll
    PUSHRM_TARGET: ${DRONE_REPO}
  when:
    status:
      - success

- name: slack
  image: themaz/drone-slack
  settings:
    webhook:
      from_secret: slack_hook
  when:
    status: [ success, failure ]
  depends_on:
    - pushrm-dockerhub

# No paths filter here on purpose - this pipeline doesn't build anything
# itself, it only runs after the four build-* pipelines above (which do
# have the filter), so it's already implicitly gated by whether those
# ran for this particular push.
trigger:
  branch:
    - master
  event:
    - push
    - custom
  status:
    - success
    - failure
